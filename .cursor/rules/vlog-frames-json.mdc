---
description: Generate and validate vlog frames JSON with project schema
alwaysApply: true
---

# Vlog Frames JSON Workflow

- 当用户要求生成用于 `vlog-pipeline` 的 `frames.json` 时，必须输出 `meta + frames` 根结构。
- `meta.voice` 只能使用项目白名单语音；`meta.rate` 必须是百分比字符串；`meta.topic` 不能为空。
- `frames` 必须 3~12 条，且首帧与尾帧 `type` 必须是 `hook`。
- 每帧 `type` 允许：`hook` / `cards` / `comparison` / **`bullets`** / **`kpi`** / **`quote`**（首尾仍为 `hook`），并满足各自必填字段约束。
- `title` 与 `script` 不得包含禁止字符；`script` 长度必须符合项目最小和最大长度限制。
- 首帧（`frames[0]`）`subtitle` 必须优先写成“带动作的完整句子”，避免只写“核心要点/先说结论”这类短标签导致中间信息块过空；其他帧 `subtitle` 维持原有生成逻辑。
- 首帧（`hook`）`script` 要短平快，只讲主题与主结论；建议控制在 **60~120 字**，把细节放到后续帧展开。
- 收尾帧（最后一帧 `hook`）可选新增 `list` 字段（2~4 条）：用于直接渲染列表摘要；若提供 `list`，渲染器优先使用它，不再从 `script` 机械截取。
- `list` 每条需是精炼短句（建议 8~20 字，尽量“动作+对象”），例如“固定输入输出协议”“补齐日志与状态快照”。

## 信息密度硬约束（避免“空白展示”）

- 生成的每一帧必须“有信息量”，禁止仅有标题/标签却无正文内容。
- `cards` 帧：
  - `cards` 数组至少 2 条；
  - 每个 card 必须同时有可读 `title|label` 与非空 `desc`；
  - `desc` 必须是具体信息，禁止“待补充/略/...”等占位文本。
- `comparison` 帧：
  - `left.points` 与 `right.points` 至少一侧非空（推荐两侧都给）；
  - 若仅一侧有 `points`，另一侧应给出可比较的简要要点，避免视觉空块；
  - `insight` 不能替代主体对比内容，仅作为结论补充。
- **`bullets` 帧**（要点清单）：
  - `items` 至少 **2** 条、至多 **8** 条；每条必须为 `{"title":"...","desc":"..."}`；
  - 生成 JSON 时必须先由 LLM 基于 `desc` 提炼 `title`（左列摘要），禁止在渲染阶段再切分字符串；
  - `title` 需短而完整（建议 6~16 字，能独立表达动作/结论），`desc` 提供具体步骤或解释；
  - 每条须为具体可执行/可判断的短句，禁止占位；单条建议可控在口播两三秒内。
- **`kpi` 帧**（关键指标）：
  - 支持两种输入：
    - 单指标：**`value`**、**`label`** 必填；可选 **`unit`**、**`footnote`**；
    - 分栏指标：`kpis[]`（2~4 项），每项至少含 `title` + `value`，可选 `label`、`unit`；
  - 当 `kpis` ≥ 4 且提供 `trend_points[]`（或 `trend_title`）时，优先走 dashboard（左指标 + 右趋势）版式；
  - 当 `kpis` 为 2~3 项时，优先走 strip（横向指标条）版式。
- **`quote` 帧**（引用块）：
  - **`quote`** 必填为完整引文；可选 **`attribution`**（出处/人名/章节）。
  - `script` 必须围绕 `quote` 本体展开讲解（先复述引用核心，再给解释/动作），避免“展示内容”和“口播内容”脱节。
- 当主题是“方法/架构/流程”时，优先输出可执行信息：步骤、原则、对比结论、常见坑与建议动作。
- 默认目标：用户看到每帧都能“直接学到一个结论或动作”，而不是只看见版式。

## 排版样式多样化（强制执行）

- 生成 `frames` 时，必须显式考虑画面排版变化，避免全片同一种布局重复出现。
- 允许每帧增加可选字段 `style` 指定版式；若未指定，则由渲染器走默认样式。
- 当前推荐样式：
  - `hook`: `center`（默认）/`split`/`spotlight`
  - `cards`: `stack`（默认）/`grid`/`timeline`
  - `comparison`: `vs`（默认）/`table`
  - `bullets` / `quote`：首版无子 `style` 分支；
  - `kpi`：支持 `single` / `strip` / `dashboard`；未显式指定时按内容自动选型。
- 同一条视频中，连续两帧尽量不使用相同 `type + style` 组合。
- 当帧数 >= 6 时，至少使用 3 种不同的 `style`；当帧数 >= 9 时，至少使用 4 种。
- 样式切换优先服务于信息表达：步骤型内容优先 `timeline`，并列信息优先 `grid` 或 `table`，强调结论优先 `spotlight`。

## 受众表达风格（默认小白）

- 默认受众为零基础用户，表达必须通俗、直白，优先用白话解释专业术语。
- 每帧脚本应先给结论，再给原因或示例，避免抽象定义堆砌。
- 优先给“可以照着做”的内容：步骤顺序、判断标准、常见错误和修正建议。
- 术语首次出现时要附一句简短解释；避免连续出现多个未解释缩写。
- 内容要“丰富但不啰嗦”：确保信息密度高，同时句子可听懂、可复述。
- 语速保持项目当前默认策略，不因“小白模式”自动降速或提速；仅在用户明确指定时才调整 `meta.rate`。
- 单帧讲解必须“先解释再推进”：至少覆盖 1 个术语解释 + 1 个具体步骤 + 1 个踩坑提醒/修正动作，避免只给结论标签。
- 当用户明确要求“讲细/放慢/小白能听懂”时，单帧 `script` 目标长度提升为 160~300 字；若低于 140 字，必须补充可执行细节而不是结束该帧。
- 对流程型内容，必须写出“下一步做什么”的动作句（例如“先建表，再填字段，再跑一次校验”），禁止停留在抽象原则。

## 去 AI 味儿写作硬约束（强制执行）

- 口吻必须像“有实操经验的人在说话”，禁止“报告体/教材体/官话套话”。
- 避免高频 AI 套路开场：如“首先其次最后”“在当今时代”“不难发现”“综上所述”等模板句。
- 每帧脚本优先使用短句和口语连接词（比如“先说结论”“你可以这样做”“最容易踩坑的是”），减少长复句堆叠。
- 禁止空泛正确话：如“提升效率”“赋能增长”“全面优化”“形成闭环”这类无动作指向的表达；必须落到具体动作或判断标准。
- 每帧至少包含一个“可执行细节”：参数、步骤、触发条件、错误信号、修正动作中的任意一种。
- 允许适度“不完美口语感”，不要每句都同长度同结构；避免全篇像同一模板生成。
- 禁止连续三句以上使用相同句式开头（例如都以“通过…”“我们可以…”起句）。
- 配音文案不写“提示词味”表达：避免“作为 AI”“本文将”“以下内容将从三个方面展开”等。
- 单帧 `script` 以“听懂并能照做”为第一目标，默认建议 120~240 字；只有在明显冗余、重复或影响理解时才拆分/精简。
- 当用户点名“讲细/放慢/小白向”时，建议 160~300 字；在术语密集或流程复杂场景可放宽到 320 字。
- 节奏建议仅作参考：普通信息帧约 8~24 秒、复杂对比帧约 10~28 秒；不得仅为卡时长牺牲解释完整度。
- 明确禁止“为压时长而删关键解释”：术语定义、步骤顺序、判断标准、踩坑提醒、修正动作优先保留。

## 真人口播改写流程（生成后必须执行一次）

- 第一步：逐帧检查并替换“AI 套话词”与“空泛形容词”，改成具体动作描述。
- 第二步：给每帧补一个真实使用场景（哪怕一句），例如“新手第一次配置时”“赶项目上线前”。
- 第三步：给每帧补一个“踩坑提醒或反例”，避免只有正向结论。
- 第四步：通读全文，打散重复句式，让语气更自然，确保听起来像真人在讲而不是在念提纲。
- 完成以上 4 步后，才允许进入结构校验与最终交付。

## 小白视角巡检机制（强制执行）

- 在交付前，必须进行一次“目标用户代入巡检”：假设自己是零基础用户，逐帧判断“是否看得懂、是否能照做”。
- 巡检时至少回答三类问题：
  - 这一帧的结论是否明确，用户是否能在 1 句话内复述；
  - 是否出现未解释术语、跳步推理、结论与示例脱节；
  - 用户看完后是否知道下一步动作，还是只能“听懂但不会做”。
- 若任一帧出现“看不懂/不够具体/不可执行”，必须先重写该帧，再继续整体校验，不可直接交付。
- 巡检补充硬规则：若“压缩后更短但更难懂”，视为不通过，必须恢复或重写为更易懂版本。
- 对 `cards` 帧，巡检重点是：每个 card 是否提供具体可操作信息，而非泛化口号。
- 对 `comparison` 帧，巡检重点是：两侧差异是否足够清楚，用户能否据此做选择。
- 巡检完成后再执行 `validate_frames.py`；只有“内容巡检通过 + 结构校验通过”才算最终通过。

## Mandatory Validation Step

在交付 JSON 前，先运行：

```bash
python3 validate_frames.py <json_file_path>
```

若校验失败，必须根据错误修正后再次校验，直到通过或用户要求停止。

## Agent 自动渲染执行（方案A）

- 当用户要求“生成后直接渲染/自动跑完整链路”时，必须在 JSON 校验通过后调用统一入口：

```bash
python3 tools/agent_pipeline.py --frames <json_file_path>
```

- 禁止手写多条零散 `curl` + 人工轮询作为默认流程；应优先使用统一入口脚本返回结构化结果。
- 若入口脚本返回失败，需基于 `error_code/error_message/validation_errors` 给出下一步修复动作，不得直接宣称完成。

## 交付附加输出（成功后必给）

- 当且仅当 JSON 通过校验后，必须额外向用户返回两项文档信息：
  - 30 字以内标题（概括文档主题，适合直接展示）；
  - 200 字以内文档总结（面向小白，说明核心内容与可执行收获）。
- 标题和总结必须基于用户提供的源文档，不可脱离原文凭空扩展。
- 若 JSON 尚未通过校验，不得先返回标题与总结作为“已完成”结果。

---
description: "当用户要求自动执行完整链路时，默认采用 rich-first 交付，禁止先交最小可运行短版。"
globs:
  - "**/*frames*.json"
  - "**/tools/agent_pipeline.py"
  - "**/validate_frames.py"
alwaysApply: false
---

# Rich-First 交付规则（防止先短版）

## 触发条件

- 用户意图包含“自动执行完整链路 / 直接渲染 / 一键出视频”。
- 输入是“文档型内容”（如 `.md/.docx/.pdf`）而非一句话短主题。

## 强制要求

- 首次可交付版本不得使用“先跑通链路”的最小结构。
- 默认目标帧数：
  - 文档内容较丰富时：`9~12` 帧；
  - 普通主题内容：`7~10` 帧。
- 帧类型至少覆盖 4 类（示例：`hook/cards/comparison/bullets/kpi/quote`）。
- 首次输出前必须完成：
  1) 内容完整性自检（章节覆盖、是否可执行）  
  2) `validate_frames.py` 结构校验  
  3) 再进入 `agent_pipeline` 渲染

## 失败策略

- 若仅产出“最小可运行版”（如 3~6 帧、内容覆盖明显不足），视为未完成，不得宣称已交付。
- 若时间紧，需要先看效果，必须明确告知“这是预览草稿”，并同步给出 rich 版本生成计划。
